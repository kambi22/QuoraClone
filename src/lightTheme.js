// lightTheme.js
import { createTheme } from '@mui/material/styles';

const lightTheme = createTheme({
  palette: {
    mode: 'light',
    // Add your light mode palette options here
  },
  // Add other theme configurations as needed
});

export default lightTheme;
