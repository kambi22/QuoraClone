// darkTheme.js

import { createTheme } from "@mui/material";


const darkTheme = createTheme({
  palette: {
    mode: 'dark',
    // Add your dark mode palette options here
  },
  // Add other theme configurations as needed
});

export default darkTheme;
